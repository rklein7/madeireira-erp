version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: madeireira-db
    environment:
      POSTGRES_DB: madeireira_erp
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: madeireira-pgadmin
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@madeireira.com
      PGADMIN_DEFAULT_PASSWORD: admin
    ports:
      - "5050:80"
    depends_on:
      - postgres

volumes:
  pgdata:
